# Strata engineering metrics — first implementation increment

Recorded per the metrics requirement. Unmeasurable values are stated as
**unavailable**, **unsupported** or **not applicable**, never as zero.

## Engineering

| Metric | Value |
|---|---|
| Projects added | 4 (`Strata.Semantic`, `Strata.Analysis`, `Strata.Host.PgParser`, `Strata.Tests`) |
| Source files added (`.fs`, excluding generated) | 13 |
| Source lines added (`.fs`, excluding generated) | 1,496 |
| Files changed in this increment | 25 (excluding ROS bookkeeping) |
| Modules added | 9 |
| Tests added | 29 |
| Test result | 29 passed, 0 failed, 0 skipped |
| Build result | succeeded, 0 warnings, 0 errors (`TreatWarningsAsErrors=true`) |
| Architecture check | pass; negative-tested (injected violation → exit 1) |
| SDE structural review | 15 source files inspected, no `SDE-STRUCT-001` warnings |
| Largest source file | `ScopeResolution.fs`, 193 lines — well inside SDE's "no size concern" band |
| Defects found and fixed during the increment | 3 (F# compile-order for `AnalysisScope`/`Schema`; `Error` shadowed by `open PgSqlParser`; two CTE tests initially non-discriminating) |
| Rework caused by those defects | 1 test-design correction, 2 build fixes |
| Elapsed task time | **unavailable** — no wall-clock timer was captured for this session |
| Agent/tool cost | **unsupported** — ROS telemetry reports token and cost capabilities as unsupported for this runtime; see the capability block in `.ros/telemetry/executions/` |

## Parser / analysis

| Metric | Value |
|---|---|
| SQL statements parsed (Spike A corpus) | 34 of 36 |
| Parse failures | 2, both deliberate negative controls, each with an accurate cursor position |
| PL/pgSQL bodies parsed | 1 of 1, including a nested `EXECUTE` |
| Parser dialect version | PostgreSQL 17.5 (`PgVersionNum` 170005) |
| Semantic extraction failures | **not applicable** — extraction runs over parsed statements only; no corpus indexing exists yet (WI-0011) |
| Unsupported constructs encountered | tracked per statement via `UnmodelledConstructs`; not yet aggregated over a corpus |
| Analysis time | **not captured** — no timing harness exists |
| Graph nodes / edges / inferred relationships | **not applicable** — no graph exists yet (WI-0012) |

## Agent experiment

All Spike D metrics (input tokens, output tokens, tool calls, files opened,
retrieval calls, query correctness, incorrect joins, hallucinated columns,
repair loops, task cost, completion time) are **not captured**: the experiment
has not run and the retrieval capability it measures does not exist.

Note for when it does run: this runtime reports token and cost metrics as
*unsupported* to ROS telemetry, so `EX-STRATA-2026-D4EA` cannot rely on ROS
telemetry alone and must capture its own measurements.

## Verification performed

| Check | Command | Result |
|---|---|---|
| Build | `dotnet build Strata.sln` | succeeded, 0 warnings |
| Tests | `dotnet test tests/Strata.Tests/Strata.Tests.fsproj` | 29 passed |
| Architecture | `./scripts/check-semantic-architecture.sh` | OK |
| Architecture (negative) | inject `Npgsql` into Tier 1 | exit 1, violation reported |
| Mutation | drop CTE bindings from the scope chain | exactly 2 tests fail, then restored to green |
| ROS | `./ros validate`, `./ros registry check` | validation passed; registries current |
| SDE | `npx @echelon-foundry/sde verify` | v1.1.1 verified, no structural warnings |

## Checks NOT performed, and what that means

- **No live PostgreSQL integration test.** No server was available. Every
  `search_path` behaviour in `ScopeResolution` is therefore verified only
  against Strata's own model of the rules, not against PostgreSQL. This is the
  outstanding half of `EX-STRATA-2026-B2E8` (WI-0014).
- **No golden semantic snapshots.** Serialization does not exist yet (WI-0006),
  so `NFR-001` determinism is unverified.
- **No property/invariant tests.** The invariants are currently enforced by
  construction and covered by example tests only.
- **No Windows or macOS execution.** Only linux-x64 was run, so `NFR-005`
  cross-platform packaging is unverified.
