# Strata implementation baseline — 2026-09-11

Recorded before any Strata implementation work begins, per Phase 0 of the
implementation program.

## Git

| Item | Value |
|---|---|
| Branch | `claude/ros-bootstrap-setup-d6y4u0` |
| HEAD SHA | `3f082b9c6ce961944d81340e312940c51e185d89` |
| Tag | `strata-implementation-baseline-20260911-135126` |
| Working tree | clean — no unrelated local changes to preserve |
| Remote refs | `main` and `claude/ros-bootstrap-setup-d6y4u0` both at HEAD |

## Toolchain

| Tool | State at baseline |
|---|---|
| .NET SDK | **not present at session start**; installed 8.0.131 from the Ubuntu archive during Phase 0 |
| F# compiler | available via the installed SDK; `dotnet new console -lang "F#"` + `dotnet build` verified working |
| NuGet flat container (`api.nuget.org/v3-flatcontainer`) | reachable |
| NuGet search index (`azuresearch-usnc.nuget.org`) | **blocked by egress policy** — packages can be fetched by exact id, but not discovered by search |
| `psql` client | 16.13 |
| PostgreSQL server | not running at baseline; not yet verified |
| Docker | 29.3.1 client present; daemon not yet verified |
| Node | 22.22.2 |

## Build and test baseline

| Check | Result |
|---|---|
| Solution / project build | **not applicable** — no F# solution exists yet; Strata has no source code at baseline |
| Test suite | **not applicable** — no test project exists yet |
| Test counts | not applicable (0 projects, 0 tests) |

These are recorded as *not applicable* rather than *passing*: there is nothing
to build or test at this commit.

## ROS / SDE verification baseline

| Check | Command | Result |
|---|---|---|
| ROS validation | `./ros validate` | validation passed |
| ROS registries | `./ros registry check` | registries are current |
| SDE integrity | `npx @echelon-foundry/sde verify` | v1.1.1 verified, 18 managed files, no SDE-STRUCT-001 warnings |

## Work items relevant to Strata at baseline

| ID | Title | State |
|---|---|---|
| WI-0001 | Install SDE v1.1.1 methodology inputs | complete |

No open or blocked Strata work items existed at baseline.

## Agent / tool cost metrics at baseline

ROS telemetry is enabled and one finalized execution record exists
(`EXE-20260911T133658955Z-31c36b07`, for WI-0001). Its capability block reports
token and cost metrics as **unsupported** for this runtime rather than zero —
the runtime does not expose them to the collector. Baseline agent cost is
therefore recorded as *unavailable*, not 0.
